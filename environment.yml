name: datalad_binder

channels:
  - default
  - conda-forge

dependencies:
  - python=3.11.*
  - pip
  - datalad=0.17.*
  - pip:
      - nbgitpuller
      - bash_kernel
      - jupyter-notebookparams
      - jupyter-server-proxy
      - jupytext
